services:
  minimap:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    command: ["uv", "run", "flask", "--app", "run", "run", "--host", "0.0.0.0"]
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
